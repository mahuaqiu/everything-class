/// 定位模式处理
/// TODO: 后续任务实现
pub struct LocateMode;

impl LocateMode {
    pub fn new() -> Self {
        Self
    }
}

impl Default for LocateMode {
    fn default() -> Self {
        Self::new()
    }
}